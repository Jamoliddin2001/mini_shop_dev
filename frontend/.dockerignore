# Keep the build context small; build output and deps are produced inside the image.
node_modules/
dist/
.git/
.gitignore
.env
.env.*
*.local
*.tsbuildinfo
coverage/
.vscode/
.idea/
.DS_Store

# Docker artifacts themselves don't belong in the build context.
Dockerfile
.dockerignore
